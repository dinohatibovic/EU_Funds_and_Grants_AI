# Root requirements — agregira slojevske zavisnosti.
# Render buildCommand koristi ovaj fajl; slojevi se održavaju odvojeno:
#   ai_core/requirements.txt  → AI sloj (Gemini, ChromaDB, scraping)
#   backend/requirements.txt  → API sloj (FastAPI, auth, billing)
-r ai_core/requirements.txt
-r backend/requirements.txt
