apiVersion: v1
kind: Service
metadata:
  name: finassistbh-backend
  labels:
    app: finassistbh
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: finassistbh
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
